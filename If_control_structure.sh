#!/bin/bash

# define a variable
x=5

# use an if control structure to execute different actions based on a condition
if [ $x -gt 10 ]
then
    echo "x is greater than 10"
else
    echo "x is not greater than 10"
fi

